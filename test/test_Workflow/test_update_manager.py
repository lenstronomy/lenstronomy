from lenstronomy.Workflow.multi_band_manager import MultiBandUpdateManager


class TestUpdateManager(object):

    def setup(self):
        pass
        #self.manager = MultiBandUpdateManager(kwargs_model, kwargs_constraints, kwargs_likelihood, kwargs_params, num_bands=0)